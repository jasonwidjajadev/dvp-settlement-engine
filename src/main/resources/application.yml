spring:
  application:
    name: dvp-settlement-engine
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/dvp}
    username: ${SPRING_DATASOURCE_USERNAME:dvp}
    password: ${SPRING_DATASOURCE_PASSWORD:}
  flyway:
    enabled: true
    locations: classpath:db/migration
